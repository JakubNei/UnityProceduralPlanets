#pragma kernel main

#include "planet.cginc"

RWTexture2D<float> _planetHeightMap;


[numthreads(16, 16, 1)]
void main(uint3 id : SV_DispatchThreadID)
{
	float w, h;
	_planetHeightMap.GetDimensions(w, h);
	float2 uv = id.xy / float2(w, h);

	float3 dir = baseMapUvToDirFromCenter(uv);

	float height01 = GetProceduralHeight01(dir);	

	_planetHeightMap[id.xy] = float3(1, 1, 1) * height01;
}